name: Generate & Upload Map Context

on:
  push:
    branches:
      - master

permissions:
  contents: read
  id-token: write

jobs:
  mapcontext:
    uses: ./.github/workflows/_ci-base.yml
    with:
      mode: mapcontext
    secrets: inherit
