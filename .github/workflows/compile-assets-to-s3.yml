name: Build & Upload Frontend Assets

on:
  push:
    branches:
      - master

permissions:
  contents: read
  id-token: write

env:
  NODE_VERSION: '24'

jobs:
  build-and-upload:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install deps
        run: npm ci

      - name: Compute git version
        run: echo -n "GIT_VERSION=$(git rev-parse HEAD)" >> $GITHUB_ENV

      - name: Build assets
        # NOTE: pass flags *after* `--` so they reach your npm script
        env:
          ASSETS_BASE_URL: https://assets.keystone.guru/
        run: npm --output_version=${{ env.GIT_VERSION }} run production

      - name: Configure AWS (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::868970774940:role/ksg-github-actions
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Install AWS CLI v2
        uses: unfor19/install-aws-cli-action@v1

      - name: Upload JS
        run: |
          aws s3 cp public/js s3://${{ secrets.S3_BUCKET }}/compiled/${{ env.GIT_VERSION }}/js \
            --recursive \
            --cache-control "public,max-age=31536000,immutable" \
            --no-progress
      - name: Upload CSS
        run: |
          aws s3 cp public/css s3://${{ secrets.S3_BUCKET }}/compiled/${{ env.GIT_VERSION }}/css \
            --recursive \
            --cache-control "public,max-age=31536000,immutable" \
            --no-progress
      - name: Upload Webfonts
        run: |
          aws s3 cp public/webfonts s3://${{ secrets.S3_BUCKET }}/compiled/${{ env.GIT_VERSION }}/webfonts \
            --recursive \
            --cache-control "public,max-age=31536000,immutable" \
            --no-progress
