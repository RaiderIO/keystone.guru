class AdminMapIcon extends MapIcon {

    // Actually this class is quite empty. But I'll have it anyway for any possible later additions.
    constructor(map, layer) {
        super(map, layer, {ignore_mapping_version_suffix: false});
    }
}
