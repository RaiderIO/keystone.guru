<h3>{{clone_to_team_label}}</h3>
<div class="form-group">
    <select id="clone-to-teams-{{publicKey}}" class="selectpicker">
        {{#each teams }}
            <option value="{{public_key}}">{{name}}</option>
        {{/each}}
    </select>
</div>