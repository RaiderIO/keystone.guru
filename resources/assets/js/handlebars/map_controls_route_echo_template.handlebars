{{#each users}}
    <li class="nav-item echo_user_{{id}} user_color_{{id}}"
        {{#if self}}
        data-toggle="tooltip" title="{{member_self_title_label}}"
        {{/if}}
    >
        {{#if self}}
            <i class="fas fa-portrait"></i>
        {{/if}}
        {{name}}
    </li>
{{/each}}