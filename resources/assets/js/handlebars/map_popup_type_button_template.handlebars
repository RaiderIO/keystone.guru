<div class="form-group">
    <label class="font-weight-bold" for="map_{{map_object_name}}_edit_popup_{{property}}_{{id}}">{{label}}:</label>
    <div class="row">
        <div class="col">
            <button id="map_{{map_object_name}}_edit_popup_{{property}}_{{id}}" class="btn btn-{{buttonType}}" data-id="{{id}}">{{buttonText}}</button>
        </div>
    </div>
</div>
