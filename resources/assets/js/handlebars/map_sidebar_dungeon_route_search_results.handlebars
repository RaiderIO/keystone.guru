<div class="row">
    <div class="col">
        {{#if search_results }}
            <h5>{{ search_results_label }}</h5>
        {{else}}
            <h5>{{ no_search_results_label }}</h5>
        {{/if}}
    </div>
</div>

{{#if search_results }}
    <ul class="list-unstyled route_picker_list search_results mb-0">
        {{{ search_results }}}
    </ul>
{{/if}}
