<img src="{{ asset(sprintf('images/flags/%s.png', language()->country($code))) }}" alt="{{ $name }}"
     class="{{ config('language.flags.img_class') }}" width="{{ config('language.flags.width') }}"/>
